[package]
name = "excalidraw-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
lz-str = "0.2"
rand = "0.8"
base64 = "0.22"
regex = "1"

[profile.release]
strip = true
lto = true
